mainMod = "SUPER"
shellCall = "noctalia msg "
launchPrefix = "uwsm app -- " -- if you are not using UWSM, make this empty (e.g. "")

require("config.keybind.window_management")
require("config.keybind.launcher")
require("config.keybind.hw_ctrl")
require("config.keybind.utils")
require("config.keybind.workspace")
require("config.keybind.shell")

